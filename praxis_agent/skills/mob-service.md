---
name: mob-service
tags: [mob, merchant-onboarding, service-domain]
summary: Placeholder domain notes for the MOB (merchant onboarding) service — replace with
  real details (key flows, gotchas, common failure modes) as they become available.
---

# MOB service notes (placeholder)

This file is a placeholder for service-specific domain knowledge about MOB. Replace this
content with real details, for example:

- What "merchant onboarding" covers end-to-end (steps, states, external verifications).
- Which other services MOB depends on and how (e.g. does it call EDI? Vault paths it reads?).
- Known tricky areas / past incident patterns worth checking first.
- Relevant database tables/collections and what a "healthy" row looks like.
- Any special local-dev quirks (env vars, seed data required beyond the baseline snapshot).

Until filled in, treat this as a signal that "MOB-specific context may exist — ask the
developer for domain specifics if the generic tools/logs/code aren't enough."
